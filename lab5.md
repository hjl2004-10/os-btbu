# Lab 5 实验报告

## 实验总结

本次实验实现了死锁检测功能。主要工作包括：
1. 在 `struct proc` 中添加了死锁检测所需的数据结构：`deadlock_detect_enable` 标志，以及 `mutex_allocation`, `mutex_request`, `sem_allocation`, `sem_request` 矩阵。
2. 实现了 Banker's Algorithm (银行家算法) 的变体用于检测死锁。算法通过构建 `Work` (可用资源) 和 `Finish` (线程完成状态) 向量，模拟资源分配过程，判断系统是否处于安全状态。
3. 实现了 `sys_enable_deadlock_detect` 系统调用，允许用户动态开启/关闭死锁检测。
4. 修改了 `sys_mutex_lock` 和 `sys_semaphore_down`，在请求资源前若开启了检测，则先进行死锁检测，若发现死锁则拒绝请求并返回错误码 `-0xDEAD`。
5. 修改了 `sync.c` 中的 `mutex_lock`, `mutex_unlock`, `semaphore_up`, `semaphore_down`，在资源分配和释放的关键路径上维护 `Allocation` 和 `Request` 矩阵，确保死锁检测算法能获取准确的系统资源状态。

完成本次实验大约耗时 3 小时。

## 问答作业

### 1. 资源回收

当主线程退出时，视为整个进程退出。需要回收的资源包括：
- **内存资源**：用户态的物理页帧（代码段、数据段、堆、栈等），页表本身。
- **内核资源**：进程控制块 (`struct proc`) 中的部分内容（如文件描述符表），内核栈。
- **同步原语**：该进程创建的互斥锁、信号量、条件变量等（如果它们是进程私有的，或者随进程销毁而销毁）。
- **线程资源**：该进程管理的所有其他线程，需要强制结束它们，并回收它们的线程控制块 (`struct thread`) 占用的槽位（标记为 UNUSED），以及它们的内核栈和用户栈。

### 2. struct thread 引用位置及回收

`struct thread` 可能在以下位置被引用：
- **`proc->threads` 数组**：这是线程的主要存储位置。需要回收（标记为 `T_UNUSED`），以便槽位复用。
- **调度器队列 (Scheduler Queue)**：如果线程处于 `RUNNABLE` 状态。需要从队列中移除，否则调度器会尝试运行已销毁的线程。
- **等待队列 (Wait Queue)**：如果线程阻塞在锁、信号量或条件变量上。需要从等待队列中移除，否则唤醒操作会访问无效内存或错误线程。
- **其他线程的 `waittid`**：如果有其他线程在等待该线程退出。需要通知等待者（如设置退出码，唤醒等待者）。
- **CPU 的 `current` 指针**：当前正在运行的线程。进程退出时，当前线程会切换到调度器，不再被引用。

### 3. mutex_unlock 实现对比

- **`mutex_unlock_v1`**：
  - 先将 `m->locked` 置为 0，然后唤醒等待队列中的线程 T。
  - **区别**：在 `locked = 0` 和 T 被调度运行之间存在时间窗口。在此窗口期，如果有另一个线程 C 请求该锁，C 会发现 `locked == 0` 并成功获取锁。
  - **问题**：当 T 最终被调度运行时，它认为自己被唤醒是因为获得了锁（通常 `mutex_lock` 在唤醒后会假设已获得锁或重新尝试）。如果 T 假设已获得锁并继续执行，就会出现 **两个线程 (C 和 T) 同时持有锁** 的情况，违反了互斥性。

- **`mutex_unlock_v2`**：
  - 直接取出等待队列中的线程 T，将其唤醒。如果队列为空，才将 `m->locked` 置为 0。
  - **区别**：锁的所有权直接从当前线程传递给 T，`m->locked` 始终保持为 1（除非没有等待者）。
  - **优势**：消除了竞争窗口。即使有线程 C 在此时请求锁，它会发现 `locked == 1` 而被迫等待。T 被唤醒时，确信自己继承了锁的所有权。这是更安全、正确的实现（通常称为 "Handoff" 语义）。

## 荣誉准则

我保证，我独立完成了本次实验，没有抄袭他人的代码或思路。参考了实验指导书和助教提供的框架代码。
